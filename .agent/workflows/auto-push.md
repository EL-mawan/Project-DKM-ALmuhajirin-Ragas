---
description: Automatically stage, commit, and push all changes to the remote repository.
---
// turbo-all
1. Stage all changes
```bash
git add .
```

2. Commit changes with a generic message or one provided by the session
```bash
git commit -m "Auto-update from Antigravity"
```

3. Push to the remote branch
```bash
git push origin main
```
